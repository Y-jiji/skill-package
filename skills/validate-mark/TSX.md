# Validated-form upgrade — TypeScript React

Extensions: `.tsx`
Items: `function_declaration`, `method_definition`, `class_declaration`, `interface_declaration`.

Upgrade rule is identical to `.ts` — see [TS](TS.md). JSX adds no behavioural difference for the docblock pipeline; the same `/* … */` → `/** … */` marker swap applies.

See `skills/act/TSX.md` for the downgrade direction.
