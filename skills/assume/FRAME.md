This note tells you how to convert your question: 
+ `What/Who/Where/When/Why/Which/Whose/Whom/How` questions
+ `Yes/No` questions

Is it possible to give a unique/definitive answer?
+ Before answering this, explore viable references exhaustively. 
+ If yes, convert to `Yes/No` question:
    + If it is not a `Yes/No` question, just keep original form. 
    + Otherwise, explore viable options, pick one option that to form a `Yes/No` question. 
+ If no, convert to `Which` question: 
    + This is likely a design choice question. 
    + Present all options with pros and cons of each to user. 

## Examples

Example of good framing:  
+ In claude code, can agent read PostToolUse hook message from "systemMessage" field?
+ Linear scan and binary search can both find insertion position in a sorted list. Which option do you want? 
    - Linear scan: how it works ...
    - Binary search: how it works...
